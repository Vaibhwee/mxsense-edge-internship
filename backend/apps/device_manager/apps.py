from django.apps import AppConfig


class DeviceManagerConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.device_manager"
    verbose_name = "Device Manager"
